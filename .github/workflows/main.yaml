name: hello

on:
  push:
    branches:
      - main
      - develop

jobs:
  job1:
    runs-on: local
    steps:
      - name: first
        uses: actions/checkout@v4
      - name: message
        run: docker run hello-world

